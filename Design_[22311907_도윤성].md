# 1. Design

## [ Revision history ]

| Revision date | Version # | Description |
| :---: | :---: | :---: |
|   03/06/2026  | 1.0.0 | 초안 |

# 2. Introdudction

본 문서는 '개인형 수면 데이터 분석 및 시각화 시스템(Sync Rhythm)'의 실제 소프트웨어 구현을 위한 세 번째 단계인 Design(설계) 명세서이다. 본 설계 단계에서는 앞서 수행된 요구사항 분석 단계의 개념적 도메인 모델들을 구체적인 객체지향 아키텍처(Class)로 변환하고 , 비즈니스 로직의 동적 흐름(Sequence)과 시스템 상태 변화(State Machine)를 정밀하게 모델링한다. 이를 통해 설계서의 논리적 요소와 실제 타이핑될 소스코드 인터페이스 간의 완벽한 추적 가능성 및 일치성을 확보하는 것을 최우선 목적으로 둔다.


# 3. Class diagram

<img width="955" height="998" alt="image" src="https://github.com/user-attachments/assets/d96829c6-2813-44b6-a4d9-9068c36d3914" />

### 1) SleepRecord
| Attributes |
| :-- |
| sleep: String: 사용자가 잠든 취침 시각 |
| wake: String : 사용자가 잠에서 깬 기상 시각 |
| score: int : 사용자가 주관적으로 평가한 기상 후 컨디션 점수 |
| duration: int : 취침 시각과 기상 시각의 차이로 도출된 총 수면 시간 |

| Description |
| :-- |
| SleepRecord 클래스는 Model의 역할을 한다. 사용자의 일일 수면 데이터를 메모리 상에 객체 형태로 담아두기 위한 순수 데이터 구조 클래스이다. |

### 2) AppDB
| Attributes |
| :-- |
| records: Map<String, SleepRecord> : 날짜 문자열(예: "2026-06-03")을 Key로 하고 SleepRecord 객체를 Value로 가지는 메모리 저장소 |

| Methods |
| :-- |
| save(date: String, record: SleepRecord): void : 특정 날짜를 키로 하여 새로운 수면 기록 객체를 저장하거나 기존 기록을 덮어씌움 |
| delete(date: String): void : 매개변수로 받은 날짜에 해당하는 수면 기록 데이터를 저장소에서 완전 삭제함 |

| Description |
| :-- |
| AppDB 클래스는 로컬 데이터베이스의 역할을 수행한다. 프로토타입 시스템 내에서 사용자의 수면 기록 데이터를 영구적/임시적으로 보관하고 데이터의 C·R·U·D를 총괄하는 데이터 계층의 핵심 클래스이다. |


### 3) SleepManager
| Methods |
| :-- |
| calculateDurationMinutes(sleep: String, wake: String): int : 입력받은 취침/기상 시각을 분 단위로 환산하고, 자정을 넘긴 경우를 예외 처리하여 총 수면 시간을 계산함 |
| saveData(): void : 뷰(UI)로부터 입력된 값을 가져와 데이터 유효성을 검증한 뒤 AppDB에 저장을 요청함 |
| deleteData(): void : 현재 뷰에 지정된 날짜 데이터를 AppDB에서 삭제하도록 요청함 |
| loadToPanel(dateStr: String): void : 달력에서 특정 날짜를 클릭했을 때 해당 날짜의 데이터를 AppDB에서 불러와 입력 폼에 바인딩함 |

| Description |
| :-- |
| SleepManager 클래스는 Controller의 역할을 한다. 사용자의 입력 값을 바탕으로 총 수면 시간을 계산하는 등 비즈니스 로직을 수행하며, 뷰(UI)와 모델(DB) 사이에서 데이터를 가공하고 전달하는 매개체이다. |

### 4) Visualizer
| Methods |
| :-- |
| renderCalendar(): void : AppDB의 데이터를 바탕으로 현재 선택된 월의 캘린더 UI를 렌더링하고, 점수에 따른 색상 박스를 동적으로 생성함 |
| renderAnalysis(): void : 선택된 주차의 7일 치 데이터를 분석하여, 최적 수면 시간 평균을 산출하고 불렛저널 형식의 막대그래프를 화면에 그림 |
| updateWeekDropdown(): void : 분석 화면 상단의 주차 선택 드롭다운(Select) 목록을 해당 월의 일자 기준에 맞게 계산하여 갱신함 |
| timeToPercentage(timeStr: String): float : 시간 문자열(HH:mm)을 차트의 Y축 비율(%)에 맞춰 소수점 값으로 변환해 줌 |

| Description |
| :-- |
| Visualizer 클래스는 View 렌더러 역할을 한다. 가공된 수면 통계 데이터를 넘겨받아 사용자가 직관적으로 인식할 수 있는 시각적 그래픽(캘린더, 주간 막대그래프)으로 변환하여 DOM에 출력해 주는 엔진 클래스이다. |

### 5) UIController
| Attributes |
| :-- |
| currentScore: int : 현재 패널에서 사용자가 선택한 컨디션 점수의 상태값 (기본값: 5) |

| Methods |
| :-- |
| initTimeSelectors(): void : 24시간제 시간/분 선택 드롭다운 폼을 동적으로 생성하고 초기화함 |
| switchTab(viewId: String): void : 캘린더 탭과 주간 분석 리포트 탭 간의 화면 전환(라우팅)을 처리함 |
| setScore(score: int): void : 컨디션 점수 버튼 클릭 시 CSS 클래스를 변경하여 선택된 상태를 UI에 반영함 |
| showToast(msg: String): void : 데이터 저장 및 삭제 시 사용자에게 일시적인 팝업(토스트) 알림 메시지를 띄움 |
| setTimeSelects(type: String, timeStr: String): void : 문자열 기반의 시간을 시간/분 드롭다운 UI 값으로 분리하여 세팅함 |
| getTimeFromSelects(type: String): String : 시간/분 드롭다운 UI의 선택값을 다시 "HH:mm" 형태의 문자열로 병합하여 가져옴 |

| Description |
| :-- |
| UIController 클래스는 프론트엔드의 화면 상태와 이벤트를 제어한다. 탭 전환, 팝업 출력, 폼 데이터 추출 및 세팅 등 사용자와의 직접적인 상호작용을 담당하는 클래스이다. |


# 4. Sequence diagram
### 1) 수면 기록 저장 및 수정
<img width="1195" height="810" alt="image" src="https://github.com/user-attachments/assets/73c8998a-8cb4-4188-ac3b-6fa1b8f9e970" />

 위의 그림은 사용자가 메인 화면의 좌측 제어 패널에서 수면 데이터를 입력하고 저장할 때의 흐름이다. 사용자가 입력을 마치고 버튼을 누르면 SleepManager가 UIController로부터 입력값을 읽어 들여 calculateDurationMinutes()를 통해 총 수면 시간을 분 단위로 환산한다. 모든 값이 유효하면 가공된 데이터가 AppDB에 영구적으로 기록되며, 완료 후 성공 토스트 메시지를 화면에 출력한다. 

### 2) 과거 수면 기록 삭제
<img width="1163" height="886" alt="image" src="https://github.com/user-attachments/assets/f2687ce2-c3f6-4d70-beb0-e80bca5bcf73" />

 위의 그림은 기입력된 특정 날짜의 수면 데이터를 삭제할 때의 흐름이다. 삭제 명령이 트리거되면 SleepManager는 AppDB에서 해당 일자의 데이터가 존재하는지 먼저 확인한다. 데이터가 비어있다면 오류 팝업을 띄우고 작업을 중지하며, 존재할 경우 AppDB에서 해당 객체를 영구 제거한 뒤 완료 알림을 띄운다.

### 3) 수면 캘린더 동기화 및 데이터 조회
<img width="1192" height="685" alt="image" src="https://github.com/user-attachments/assets/23bf948e-51de-457e-ba2f-d40123a79cb6" />

 위의 그림은 사용자가 조회 기준 날짜를 변경하거나 데이터 C·U·D 작업이 일어나 화면을 다시 그려야 할 때 작동하는 캘린더 시각화 흐름이다. Visualizer 모듈이 AppDB에 접근하여 해당 연/월에 해당하는 모든 날짜의 데이터를 꺼내오며, 기록이 존재하는 날짜에 한해 컨디션 점수에 따른 색상 박스를 화면 상에 그린다. 

### 4) 주간 최적 수면 효율 분석 시각화
<img width="1155" height="1084" alt="image" src="https://github.com/user-attachments/assets/457bf774-f3db-4208-9334-c82e794082db" />

 위의 그림은 본 시스템의 코어 기능인 주간 수면 분석 리포트가 도출되는 과정이다. Visualizer는 사용자가 선택한 주차(Week)의 7일 치 데이터를 AppDB에서 순회하며, 점수가 4점 이상인 양질의 수면 데이터만 필터링하여 최적 수면 평균 시간을 계산한다. 동시에 취침/기상 시각을 timeToPercentage()로 Y축 위치 퍼센트로 변환하여 화면에 불렛저널 막대그래프를 동적 렌더링한다.  



# 5. State machine diagram

<img width="779" height="1299" alt="image" src="https://github.com/user-attachments/assets/5dafbc34-5c37-4297-8425-5a0fecc60d6b" />

### Description
| Status | Explanation |
| :-- | :-- |
| CalendarView | 접속 시 기본으로 진입하는 메인 상태. 좌측에는 제어 패널이, 우측에는 한 달간의 수면 분포를 보여주는 캘린더가 활성화되어 있다. |
| PanelLoaded | 캘린더에서 특정 날짜를 클릭하여, 해당 날짜의 기존 수면 기록(취침/기상/점수)이 좌측 제어 패널로 모두 불러와져(Load) 폼에 바인딩된 상태이다. |
| Editing | 사용자가 드롭다운 폼을 조작하여 시간이나 분을 변경하거나, 1~5점 버튼을 눌러 새로운 값을 세팅하고 있는 수정/작성 중인 상태이다. |
| Saving / Deleting | 편집을 마친 사용자가 저장/수정 또는 삭제 버튼을 클릭하여 내부 AppDB 저장소에 데이터가 삽입, 갱신, 삭제되고 있는 백그라운드 데이터 처리 과도기 상태이다. |
| ToastNotified | 데이터 처리 연산이 완료되어 시스템이 성공 혹은 실패에 대한 피드백 결과(토스트 팝업 메시지)를 화면 하단에 띄우고 있는 결과 알림 상태이다. 알림이 끝나면 즉시 전체 화면 뷰를 갱신하고 이전 탭 상태로 되돌아간다. |
| AnalysisView | 사용자가 네비게이션 탭에서 분석 리포트를 선택했을 때의 상태. 불렛저널 막대그래프와 최적 수면 통계 시간이 화면에 렌더링된다. |


# 6. Implementation requirements
| Component | Minimum Requirement |
| :--: | :--: |
| CPU | Intel Core i3 + |
| RAM | 2GB |
| Storage | 50MB의 여유 공간 |
| Display | 1280*720 이상 |


# 7. Glossary
| Name | Description |
| :-- | :-- |
| Class diagram | 시스템의 정적 구조와 구성 요소들의 특성인 속성, 행위, 클래스 간의 구조적 역할을 한눈에 식별할 수 있도록 도식화한 UML 구조 다이어그램이다. |
| Sequence diagram | 개별 유스케이스 시나리오의 흐름 속에서 런타임 객체 인스턴스들이 시간의 수직적 흐름선에 의거하여 주고받는 메시지의 동적 절차 관계를 가시화해 주는 행동적 다이어그램이다. |
| State Machine Diagram | 소프트웨어 모듈이 외부 이벤트 트리거 신호를 받았을 때 라이프사이클에 걸쳐 전이될 수 있는 유한한 상태들의 흐름 구조를 나타낸 다이어그램이다. |
# 8. References
